<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SprestMapper">

	<select id="selectByS_no" parameterType="int" resultType="Sprestmember">
		SELECT * FROM sprestmember WHERE s_no = #{s_no}
	</select>

</mapper>  
   
